# mtproto-polling-service

A small Windows service (with a console run mode) that keeps a working Telegram
MTProto proxy on hand and exposes it over a local HTTP endpoint.

See issue [#1](https://github.com/eugene-pi/mtproto-polling-service/issues/1).
